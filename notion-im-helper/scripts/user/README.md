# User Configuration

This directory contains **personalized** configuration files.
These files are specific to your Notion setup and are NOT shared
when distributing the skill.

## files

- `pages.json` — Named Notion page ID mappings (e.g., "lifeos" page)
